# hacdias/webdav config. No auth here — Caddy handles basic auth in front;
# this container is only reachable on the internal docker network.
address: 0.0.0.0
port: 6065
prefix: /dav
directory: /data
permissions: CRUD
log:
  format: console
