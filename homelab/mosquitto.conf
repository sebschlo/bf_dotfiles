# MQTT broker for OwnTracks (phone publishes; recorder + Home Assistant subscribe).
# No TLS: reachable only over the tailnet / LAN, never the internet.
listener 1883
allow_anonymous false
password_file /mosquitto/data/passwd
persistence true
persistence_location /mosquitto/data/
